class Block {
  constructor(options) {
    this.image = options.image;
    this.x = options.x;
    this.y = options.y;
    this.width = options.width;
    this.height = options.height;
  }
}

module.exports = Block;
